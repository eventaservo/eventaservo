# == Schema Information
#
# Table name: participants
#
#  id         :bigint           not null, primary key
#  public     :boolean          default(FALSE), indexed
#  created_at :datetime         not null
#  updated_at :datetime         not null
#  event_id   :bigint           indexed
#  user_id    :bigint           indexed
#
# This fixture provides participants for the events in events.yml.
# It covers both sides of the participant visibility flag: one participant
# who agreed to appear publicly (public: true) and one who did not
# (public: false), both joined to the same valid_event fixture.

public_listed:
  event: valid_event
  user: user
  public: true

private_listed:
  event: valid_event
  user: teacher
  public: false