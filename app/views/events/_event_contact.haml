.d-flex
  - if @event.site.present?
    = link_to icon('fas', 'globe', 'retpaĝo'), @event.site, target: :_blank, class: 'button-contact'

  - if @event.email.present?
    = mail_to @event.email, icon('fas', 'envelope', 'retpoŝto'), subject: "Informoj pri la evento #{@event.title}", class: 'button-contact'