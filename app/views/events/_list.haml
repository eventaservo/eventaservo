.item-row-no-chevron
  .col-12
    %p
      .text-center.font-weight-bold
        = event_flag(event)
        = link_to event.title, event_path(event.code)
      .text-center
        = event.online ? "(Reta evento)" : "(#{event.city} - #{event.country.name})"
        = event_date(event)
    %p.text-center= event.description
