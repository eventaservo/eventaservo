<%= render partial: 'events/timezone_modal' %>
<%= link_to "#{cookies[:horzono] ? t("timezone.change_timezone") : t("timezone.select_timezone")}", '#', id: 'select-timezone-link',
  class: 'small', data: { bs_target: '#timezoneModal', bs_toggle: 'modal'} %>
