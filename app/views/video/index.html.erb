<div class="box-white" id="evento">
  <div class="lead">
    <%= icon('fas', 'video', 'Registritaj prezentoj') %>
  </div>
  <div class="row">
    <%- @videoj.each do |video| %>
      <div class="col-12 col-md-6 col-lg-4">

      </div>
    <% end %>
  </div>
  <div class="d-flex justify-content-center small pt-3">
    <%= raw pagy_bootstrap_nav(@pagy) %>
  </div>
</div>
