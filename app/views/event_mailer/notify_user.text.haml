Nova evento en #{@event.country.name} aldoniĝis ĉe Eventa Servo. Jen pliaj detaloj:
\
\
= @event.title.upcase
\------------------
#{@event.city} (#{@event.country.name})
=format_date(@event.date_start)
- if @event.date_end > @event.date_start
  ĝis #{format_date(@event.date_end)}
\
= @event.description
\
= @event.content
\
Kreinto: #{@event.user.name}
\
Vidu la eventon rekte en la paĝo:
= event_url @event.code
\
\
Se vi ne plu volas ricevi informojn pri novaj eventoj en #{@event.country.name}, alabonigu per tiu ligilo:
= delete_recipient_url @recipient.code
