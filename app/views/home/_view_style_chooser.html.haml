.text-center
  .btn-group.btn-group{role:"group", "aria-label": "grupo2"}
    = link_to icon('fas', 'list'), view_style_path('listo'), class: "btn #{session[:event_list_style] == 'listo' ? 'btn-primary' : 'btn-light' }"
    = link_to icon('far', 'square'), view_style_path('kartoj'),  class: "btn #{session[:event_list_style] == 'kartoj' ? 'btn-primary' : 'btn-light' }"
    = link_to icon('far', 'calendar'),view_style_path('kalendaro'), class: "btn #{session[:event_list_style] == 'kalendaro' ? 'btn-primary' : 'btn-light' }"
