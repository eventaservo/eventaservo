<div class="box-white">
  <div class="lead">
    Eventoj kun raportoj
  </div>

  <%= render "events_with_report_list", events: @events %>

  <div class="d-flex justify-content-center small">
    <%= @pagy.series_nav(:bootstrap).html_safe %>
  </div>
</div>
