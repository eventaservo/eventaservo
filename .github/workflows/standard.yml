name: Standard Ruby

on: [pull_request]

jobs:
  checker:
    runs-on: ubuntu-22.04
    permissions:
      checks: write
      contents: write
    steps:
    - name: Standard Ruby
      uses: standardrb/standard-ruby-action@v1
